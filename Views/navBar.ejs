<nav>
    <h1>Good Reads</h1>
    <ul>
        <% links.map(link => { %>
            <li>
                <a href="<%= link.href %>"">
                    <span><%= link.text %></span>
                </a>
             </li>
        <% }) %>
    </ul>
</nav>